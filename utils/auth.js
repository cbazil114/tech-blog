// Check if logged in. If not, redirect to login page. Otherwise, continue to designated page. 
const withAuth = (req, res, next) => {
    if (!req.session.logged_in) {
      res.redirect('/login');
    } else {
      next();
    }
  };
  
  module.exports = withAuth;
  